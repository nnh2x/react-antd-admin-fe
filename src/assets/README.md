## Static Files Directory

- The `images` folder is used to store image files such as JPG and PNG; if there are too many images, you can add subfolders to organize them by category
- The `svg` folder is used to store SVG files
……
